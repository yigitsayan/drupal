{% set heading_id = attributes.id ~ '-menu'|clean_id %}
<nav role="navigation" aria-labelledby="{{ heading_id }}" class="main-header__navigation">
  <div class="container">
    <div class="row">
      <div class="col-md-offset-4 col-md-4 col-sm-offset-4 col-sm-4 hidden-xs">{% block content %}
      {{ content }}
    {% endblock %}</div>
    </div>
  </div>
</nav>